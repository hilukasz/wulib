#target Illustrator// @include lukaszLibrary.jsx;var divExists = false;alert(numberOfLayers);for (var i = 0; i < numberOfLayers; i++) {     var customName = "div structure";    var myLayer = app.activeDocument.layers[i];    var myLayer = myLayer.name;    alert(myLayer);    //alert (myLayer);    if (customName == myLayer) {        divExists = true;        var divHeightWidth = [];        //getObjectWidthHeight(i);        writeCSV();    }    if (divExists == false) {        alert ("you, you crazy or something mang?! add a layer called \"div structure \" else this wont work");        }}var iCount =  doc.symbolItems.length;var dest = undefined;var dest;exportSymbolasPNG();alert("BAM. WAM. KAZAAAAAAM."+"\n"+" You just saved a lot of time.");